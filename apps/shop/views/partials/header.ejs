<!doctype html>
<html lang="ja">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= title || "Vulnerable Shop" %></title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <div class="container">
      <header>
        <div><strong>Vulnerable Shop</strong> <span class="muted">（学習用・本番利用厳禁）</span></div>
        <nav>
          <a href="/products">商品一覧</a>
          <a href="/search">商品検索</a>
          <a href="/hands-on">Hands-on</a>
          <a href="/orders">注文履歴</a>
          <% if (currentUser) { %>
            <span class="muted">ログイン中: <%= currentUser.username %></span>
            <a href="/logout">ログアウト</a>
          <% } else { %>
            <a href="/register">登録</a>
            <a href="/login">ログイン</a>
          <% } %>
        </nav>
      </header>
