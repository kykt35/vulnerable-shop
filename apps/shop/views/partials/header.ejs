<!doctype html>
<html lang="ja">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= title || "Vulnerable Shop" %></title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <div class="container">
      <header class="topbar">
        <div class="brand">
          <span class="pill">学習用</span>
          <div>Vulnerable Shop</div>
          <span class="muted">（本番利用厳禁）</span>
        </div>
        <nav class="nav-links">
          <a class="nav-link" href="/products">商品一覧</a>
          <a class="nav-link" href="/search">商品検索</a>
          <a class="nav-link" href="/hands-on">Hands-on</a>
          <a class="nav-link" href="/orders">注文履歴</a>
          <% if (currentUser) { %>
            <span class="nav-link muted">ログイン中: <strong><%= currentUser.username %></strong></span>
            <a class="nav-link primary" href="/logout">ログアウト</a>
          <% } else { %>
            <a class="nav-link" href="/register">登録</a>
            <a class="nav-link primary" href="/login">ログイン</a>
          <% } %>
        </nav>
      </header>
      <main class="page">
